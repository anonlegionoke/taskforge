{
    "name": "@taskforge/shared",
    "version": "1.0.0",
    "main": "src/index.ts",
    "types": "src/index.ts",
    "dependencies": {
        "amqplib": "^1.0.3",
        "node-pg-migrate": "^8.0.4",
        "pg": "^8.20.0"
    },
    "devDependencies": {
        "@types/amqplib": "^0.10.8",
        "@types/pg": "^8.20.0"
    },
    "scripts": {
        "migrate:create": "NODE_OPTIONS='--import tsx' node-pg-migrate -m src/migrations -j ts create",
        "migrate:up": "NODE_OPTIONS='--import tsx' node-pg-migrate -m src/migrations -j ts up"
    }
}
